server:
  port: 8090
spring:
  jpa:
    hibernate: 
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/pokemonDB?zeroDateTimeBehavior=convertToNull&serverTimezone=UTC
    username: ${MYSQL_USER:ftorres}
    password: ${MYSQL_PASSWORD:.d1s3n081}